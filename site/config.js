/* Written at publish time by the workflow. Values here are the local defaults. */
window.DASHBOARD_CONFIG = {
  repo: "aakashchamola/automated-scraping",
  workflow: "scheduled-pipeline.yml",
  branch: "automateV2",
  builtAt: null,
};
